{
  "name": "task-hierarchy",
  "version": "1.0.0",
  "description": "3-level task management app",
  "main": "index.js",
  "scripts": {
    "start": "parcel public/index.html",
    "build": "rm -rf dist/ && parcel build public/index.html --no-source-maps --public-url ./ --no-cache"
  },
  "dependencies": {
    "react": "^17.0.2",
    "react-dom": "^17.0.2",
    "react-router-dom": "^6.2.1"
  },
  "devDependencies": {
    "parcel-bundler": "^1.12.5"
  }
}
